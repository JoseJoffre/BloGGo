<%- include ("../../partials/header.ejs") %>
<%- include ("../../partials/navbar.ejs") %>
<div class="container">
    <hr>
    <h2>Categorias</h2>
    <a class="btn btn-success" href="/admin/users/create">Criar Novo Usuário</a>
    <hr>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>E-mail</th>

            </tr>
        </thead>
        <tbody>

            <% users.forEach(user => { %>
            <tr>
                <th><%= user.id %></th>
                <th><%= user.email %></th>


            </tr>
            <%})%>
        </tbody>
    </table>
</div>

<%- include ("../../partials/footer.ejs") %>
<script>
    function confirmDelete(event, form) {
        event.preventDefault();
        const decision = confirm("Tem certeza que quer deletar esta categoria para sempre?")
        if (decision) {
            form.submit();

        } else {

        }
    }
</script>